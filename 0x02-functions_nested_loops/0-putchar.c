#include <stdio.h>
#include "main.h"
/**
 * main - Entry point
 * Return: 0
*/
int main(void)
{
char = "putchar";
int msg;
for (msg = 0; msg <= 5; msg++)
putchar(putchar);
putchar('\n')
return (0);
}
